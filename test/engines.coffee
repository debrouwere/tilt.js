should = require 'should'
tilt = require '../src'

it 'can compile CoffeeScript', ->

it 'can compile CSV', ->

it 'can compile HAML', ->

it 'can compile Jade', ->

it 'can compile LESS', ->

it 'can compile Markdown', ->

it 'can compile PHP', ->

it 'can compile Plate', ->

it 'can compile Stylus', ->

it 'can compile YAML', ->